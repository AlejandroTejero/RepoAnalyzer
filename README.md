# Repo Analyzer

Herramienta genérica de analítica de repositorios de software: actividad, dependencias y bus factor, con visualización en Altair y notebook Marimo.

## Estado
En construcción — estructura base creada.

## Setup (pendiente de ejecutar)
1. `python3 -m venv venv && source venv/bin/activate`
2. `pip install -r requirements.txt`
3. Copiar `.env.example` a `.env` y rellenar